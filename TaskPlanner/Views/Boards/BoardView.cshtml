@model TaskPlanner.Models.Board

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Board</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
    </dl>
    <dl class="row">
        testttt
        @foreach (BoardColumn item in Model.BoardColumns)
        {
        <div>BoardColumn:  @item.Title</div>
        }
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.BoardId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
