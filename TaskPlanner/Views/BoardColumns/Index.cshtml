@model IEnumerable<TaskPlanner.Models.BoardColumn>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Board)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Board.BoardId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BoardColumnId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BoardColumnId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BoardColumnId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
